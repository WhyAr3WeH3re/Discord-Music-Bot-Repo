import { doActionAndLog } from '#lib/utils';
import { execSync } from 'node:child_process';
export function stageFiles(options) {
    return doActionAndLog('Staging package.json and CHANGELOG.md', //
    () => {
        if (!options.dryRun) {
            execSync('git add package.json CHANGELOG.md');
        }
    });
}
//# sourceMappingURL=stage-files.js.map