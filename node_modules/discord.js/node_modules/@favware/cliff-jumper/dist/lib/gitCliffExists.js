/* eslint-disable */
import { Result } from '@sapphire/result';
import { execSync } from 'node:child_process';
export function gitCliffExists() {
    const result = Result.from(() => execSync('git cliff', { encoding: 'utf-8', stdio: ['ignore', 'ignore', 'pipe'] }));
    if (result.isErrAnd((error) => error.stderr.includes('is not a git command'))) {
        return false;
    }
    return true;
}
//# sourceMappingURL=gitCliffExists.js.map