import { Result } from '@sapphire/result';
import { writeFile } from 'node:fs/promises';
export async function createFile(path) {
    const result = await Result.fromAsync(() => writeFile(path, ''));
    return !result.isErr();
}
//# sourceMappingURL=createFile.js.map